import { Container } from 'react-bootstrap';

const FooterContainer = () =>
    <footer className='fixed-bottom'>
        <Container fluid='sm'>
            <span className="text-muted">Manfredas Liutkus. Anesthetic Chart in React. All Rights reserved </span>
        </Container>
    </footer>;

export default FooterContainer;
